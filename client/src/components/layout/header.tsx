import React from 'react';
import NavBar from './nav-bar';

const Header: React.FC = () => {
  return (
    <header>
      <div>Logo</div>
      <NavBar />
    </header>
  );
};

export default Header;
