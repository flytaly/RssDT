import LoginCard from '../components/login/login-card';
import formTypes from '../components/login/form-types';

const RequestPasswordPage = () => <LoginCard form={formTypes.request_password} />;

export default RequestPasswordPage;
