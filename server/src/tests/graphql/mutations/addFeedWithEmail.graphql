mutation addFeedWithEmail($email: String!, $feedUrl: String!) {
    addFeedWithEmail(email: $email, feedUrl: $feedUrl) {
        userFeed {
            id
            activated
            feed {
                id
                url
            }
        }
        errors {
            message
            field
        }
    }
}
