mutation addFeedToCurrentUser($feedUrl: String!) {
    addFeedToCurrentUser(feedUrl: $feedUrl) {
        userFeed {
            id
            activated
            feed {
                id
                url
            }
        }
        errors {
            message
            field
        }
    }
}
