mutation addFeedToCurrentUser($feedUrl: String!) {
    addFeedToCurrentUser(input: { feedUrl: $feedUrl }) {
        userFeed {
            id
            activated
            feed {
                id
                url
            }
        }
        errors {
            message
            argument
        }
    }
}
