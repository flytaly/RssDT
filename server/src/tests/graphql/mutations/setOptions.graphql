mutation setOptions($opts: OptionsInput!) {
    setOptions(opts: $opts) {
        options {
            ...optionsFields
        }
        errors {
            message
            argument
        }
    }
}
